ich will einen minecraft mod coden mit folgenden funktionen:
mit dem playerlistmixin werden die spieler erkannt und dann überprüft. wenn sie in der datenbank des servers ein cape haben, sollen sie es bekommen. man soll auch die spieler auf den kopf stellen können und ihnen wie deadmaus5 große ohren geben können oder dem cape den chanted glint geben kann. sehr wichtig: ich brauche auch eine reload funktion.

erkläre mir ganz genau wie ich es programmieren soll, mit client und serverseite, datenstruktur und jeden files. der mod ist für fabric 1.21
die datei soll per http von meinem webserver abgefragt werden. die spieler werden per uuid überprüft.
es soll auch animated capes geben die dann einfach aufgebaut sind wie cape texturen als gif

es soll eine verbindung mit einem server geben damit ich mit einem command einen befehl senden kann der an alle clients geschickt wird der überall nen reload auflöst.
ich als owner habe einen command /pixelcapes <code> set <playername> cape/ears/flipped true/false/texturelink
wenn es den player noch net gibt wird er entfernt und wenn ich gar keine sache mehr true hab und keine textur, wird er gelöscht. maybe später so das es auch infos gibt wer welches cape besitzt(später)
der code ist einfach ein token wie google authenticator

schreibe den gesamten code für jedes file mit imports etc und schreibe auch den teil zum rendern der sachen

teile es schön übersichtlich in verschiedene dateien auf und achte darauf nur gültige java packets zu importieren

die datei auf dem server mit der zuweisung soll so sein:
{
    "players": {
        "<player1_name>": {
            "uuid": "playeruuid",
            "cape": true,
            "ears": false,
            "flipped": false,
            "enchanted_cape": false,
            "cape_texture": "direkter link zur textur",
            "ears_texture": null
        },
            "<player2_name>": {
                ...
            }
    }
}